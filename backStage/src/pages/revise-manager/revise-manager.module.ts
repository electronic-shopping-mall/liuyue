import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ReviseManagerPage } from './revise-manager';

@NgModule({
  declarations: [
    ReviseManagerPage,
  ],
  imports: [
    IonicPageModule.forChild(ReviseManagerPage),
  ],
})
export class ReviseManagerPageModule {}
